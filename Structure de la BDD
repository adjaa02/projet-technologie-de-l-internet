CREATE DATABASE senegal_tourisme;

USE senegal_tourisme;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password VARCHAR(255) NOT NULL
);

INSERT INTO users (username, email, password) VALUES
('admin', 'admin@example.com', 'mdp123'),
('bouba_fall', 'bouba@example.com', 'bouba2003'),
('adja_diouf', 'adja@example.com', 'adja1996');
